{
  "name": "hemlock-bay",
  "version": "0.1.0",
  "description": "Hemlock Bay — an archival static site.",
  "private": true,
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
